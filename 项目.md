项目实战

## 目录

- [6.1 小智 AI 语音聊天机器人](#61-小智-ai-语音聊天机器人)
- [6.2 智能门铃](#62-智能门铃)
- [6.3 智能门锁](#63-智能门锁)
- [6.5 牛羊定位器](#65-牛羊定位器)
- [6.6 自行车码表](#66-自行车码表)
- [6.7 STM32网关与步进电机控制](#67-stm32网关与步进电机控制)
- [6.8 Linux 智能网关](#68-linux-智能网关)
- [附录A：项目串讲模板](#附录a项目串讲模板)
- [附录B：C语言速查表](#附录bc语言速查表)
- [附录](#附录)

---

### 6.1 小智 AI 语音聊天机器人

#### 项目背景

一款 AI 语音聊天机器人，支持语音交互和智能回复，可以进行日常聊天和设备操控（如查询天气、调节音量）

#### 硬件配置

| 组件           | 型号     | 参数                                                         | 通信方式  |
| -------------- | -------- | ------------------------------------------------------------ | --------- |
| 主控           | ESP32-S3 | 主频 240MHz，内存 512KB，Flash 8MB，外扩 SPIRAM 8MB  双核 (大量环形缓冲区) | -         |
| 音频编解码器   | ES8311   | 通过adc采集模拟信号  模拟信号给es8311 编解码处理             | I2C + I2S |
| 功放(播放音频) | NS4105B  | 模拟音频信号放大                                             | -         |
| 屏幕           | ST7789V  | 320×240 分辨率 IPS  lcd                                      | SPI       |

硬件: 主控: esp32-s3  主频:240MHz 内存:512k flash:8M  双核
 es8311: i2c和i2s  音频编解码器   录音和播放

 屏幕: 简单的lvgl  显示聊天内容16   配网二维码 emoji表情    时间 对话内容  wfii连接  电池电量  说话状态



代码约4000行    **烧录固件**：约 **4.75 MB** 

#### 软件架构

##### 1. 智能配网

| 方式       | 技术               |
| ---------- | ------------------ |
| 低功耗蓝牙 | BLE 配网           |
| WiFi 配网  | 热点 + HTTP Server |

##### 2. 数据流程

**上行流程（录音上传）**：

通过adc采集模拟信号  模拟信号给es8311 编解码处理

**PCM**（Pulse Code Modulation）： 都是音频数据格式 

​	脉冲编码调制，**原始未压缩音频**直接记录声波振幅的数值,数据量大（16kHz采样率，每秒32KB），但音质无损,麦克风采集和扬声器播放的都是PCM

**Opus**： 一帧60ms

- **高效音频压缩编码格式**专为实时通信设计（网络传输用）,低延迟、高压缩率（相比PCM可压缩10倍以上）智能区分语音和音乐，自动优化

  

1. **adc采集麦克风模拟信号** → ES8311 ADC 转换 → **I2S读取PCM数据**
2. **es2buff任务** → 从ES8311读取512字节PCM → 存入 `es8311_to_sr_buffer` 环形缓冲区
3. **buff2sr任务** → 取出PCM → 送入**本地SR模型**（唤醒词识别 + VAD人声检测）
   - 未检测到唤醒词 → 丢弃数据
   - 检测到唤醒词 → 触发回调，设置状态机为聆听  `is_wakup=true`
4. **sr2buffer任务** → 唤醒后的人声数据 → 存入 `sr_to_encoder_buff` 缓冲区
5. **BUFF2encoder任务** → PCM数据 → **pcm格式转成Opus编码(1帧60ms)**（压缩）→ 存入 `encoder_to_ws_buff` 缓冲区
6. **upload任务** → 检查 `communicationStatus==LISTENING` → 取出Opus数据 → **WebSocket发送**到服务器

- 只有检测到唤醒词后，VAD检测到用户说话状态切换 (（`SILENCE_TO_SPEECH`），数据才会进入编码和上传环节

- 用户说完（`SPEECH_TO_SILENCE`），停止上传，等待服务器回复

- **VAD**（Voice Activity Detection，语音活动检测）的原理：

  VAD通过分析音频信号的**能量/幅度**来判断是否有人声，在回调函数中处理，避免把环境噪音发给服务器，只有真正的人声才上传  

**下行流程（播放回复）**：

1. **WebSocket接收** → 服务器下发Opus格式的AI语音数据 (音频文本)→ 触发`App_Communication_WebsocketReceiveHandle`
2. **存入缓冲区** → 音频数据（二进制）写入 `ws_to_decoder_buff` 环形缓冲区（32KB）
3. **decoder任务解码** → `App_Audio_BufferToDecoderTaskFunc` 从缓冲区取出Opus数据 → 调用 `Inf_Decoder_Decode()` **解码成PCM格式**，文本显示在lvgl
4. **ES8311 DAC转换** → PCM数字信号送入ES8311 → **DAC（数模转换）** 转成模拟音频信号
5. **扬声器播放** → 模拟信号输出到扬声器，用户听到AI的回复





在本地唤醒，只上传有人声的 ，靠sr模型的vad检测人声

vad配置

opus格式 1帧60ms 服务器需求

卡顿     编码解码放在cpu不同核心 提高相应任务优先级

##### 3. 状态机

语音识别状态、唤醒状态、睡眠状态的切换，使用全局标志位实现。

 状态切换由**VAD事件**和**服务器消息**共同驱动。 





 偶发性死机、内存泄露、任务阻塞 

#### 面试问题：

```
 一 ：播放卡顿 , AI回复音频播放卡顿，出现"爆破音"。 Opus编解码实时性不足
原因：编解码任务与其他任务抢占CPU，
解决：
编解码任务绑定到Core 1（xTaskCreatePinnedToCoreWithCaps）
WiFi相关任务在Core 0运行（ESP32默认）
增加任务优先级：decoder(7) > encoder(6) > upload(6)
使用PSRAM存储中间数据，减少内部RAM压力

二：  网络波动引发的状态机错乱（最难复现）
现象：用户反馈"，断网重连后有时候会叫了没反应"或"AI一直在说话，打断不了"，重启后恢复正常。

根本原因：
WiFi重连时，WebSocket未完全断开就重新连接，导致双连接，设备收到两份数据导致错乱崩溃
tts/stop 消息在重连时丢失，communicationStatus 卡在 SPEAKING
状态切换依赖服务器消息，网络延迟时用户操作（按键）与状态不同步
检测方法：
状态停留超30秒自动报警（SPEAKING/LISTENING不应这么久）

预防措施，解决方法：
WebSocket重连时强制重置所有状态标志和缓冲区
唤醒时检查当前状态，强制回到IDLE再开始

三:  偶发性内存泄漏导致的随机重启
现象：设备运行几小时到几天后突然重启，日志显示 Heap exhausted 或 Stack overflow，但刚启动时内存充足。
	为什么难定位：
	根本原因：

session_id = strdup() 每次重连都分配新内存，旧内存未释放
cJSON_PrintUnformatted 生成的字符串偶尔未 free（异常分支跳过）
WebSocket异常断开时，环形缓冲区中的数据块未回收

泄漏速度慢（几KB/小时），开发测试阶段发现不了
重启后日志丢失，无法追溯最后一次操作
与使用频率相关（对话越多，JSON解析越频繁，泄漏越快）
解决 ：内存下降到阈值时，释放内存的标签和大小
	  重连WebSocket前强制释放session_id旧内存
	 
```



Websocket是长连接，全双工、支持二进制数据帧，能够在同一条链路上传JSON + 音频，非常适合实时语音传输。且Websocket是主要面向于网页，符合大模型接口连接状态



使用I2S进行音频采集时，至少需要几根信号线，为什么？

- 最少需要三根信号线才能进行音频数据传输。
- BCLK（位时钟）：控制每一位PCM数据的传输速率。MCU或音频解码器根据这个时钟同步发送/接收每一位数据
- LRCK（左右声道选择，Word Select  WS）：决定当前传输的是左声道还是右声道。通常是每个采样周期翻转一次
- SD（Serial Data）:音频数据，实际传输PCM或压缩音频数据的串行线

#### 环形缓冲区

​	 **环形缓冲区通过首尾相连的循环指针设计，实现最低时间复杂度的读写操作，有效解耦生产者和消费者的速度差异，是音频、视频等实时数据流的最佳缓冲方案。** 

   写指针读指针

#### 智能配网

BLE配网流程是如何设计的？

- 设备进入配网状态，开启BLE广播
- App连接BLE，写入SSID、密码
- ESP32校验数据并保存到NVS中
- 切换到WiFi - STA模式连接路由器
- 成功后关闭BLE进入低功耗





#### 小智总结

```
我们做的这个AI小智是一个嵌入式AI语音交互设备，主要功能是进行人声识别并与云端AI大模型服务器进行交互，可以进行日常聊天和设备操控，比如询问今天天气、调节音量、控制灯光等。

	硬件选型方面，因为整个设备需要进行语音识别、联网和处理大量的音频数据，所以MCU我们选择了自带蓝牙和WiFi的ESP32-S3FN8，双核，主频240MHz能快速处理音频数据，内置512KB内存和8MB Flash，同时外置了8MB PSRAM用于大数据缓存。在音频编解码器选择ES8311，通过I2C进行控制，I2S进行音频数据收发，麦克风采样的PCM音频数据采样频率为16kHz、位深16位。。显示采用320x240分辨率的IPS屏幕，驱动芯片ST7789V，通过SPI与ESP32通信。
	opus格式 1帧60ms 服务器需求
软件部分的核心是将Opus格式的音频数据通过WebSocket协议与服务器进行双向实时通信。音频处理链路分为上行和下行，我们创建多个freertos任务和环形缓冲区对大量音频数据进行处理：
	数据上行流程（麦克风 → 服务器）：
	ADC采集麦克风模拟信号 →模拟信号给 ES8311编码处理pcm → I2S读取PCM数据 → es2buff任务读取512字节PCM存入环形缓冲区 → buff2sr任务送入本地SR模型（唤醒词识别 + VAD人声检测）。未检测到"小爱同学"则丢弃数据；检测到后触发回调设置is_wakup=true，sr2buffer任务将唤醒后的人声存入sr_to_encoder_buff → BUFF2encoder任务将PCM编码为Opus格式（60ms/帧）→ upload任务检查communicationStatus==LISTENING后通过WebSocket上传服务器。只有VAD检测到SILENCE_TO_SPEECH（用户开始说话）数据才会进入编码上传；检测到SPEECH_TO_SILENCE（用户说完）则停止上传，等待服务器回复。

	数据下行流程（服务器 → 扬声器）：
	服务器下发Opus格式AI语音数据及JSON文本 → WebSocket触发App_Communication_WebsocketReceiveHandle → 音频数据写入ws_to_decoder_buff环形缓冲区 → decoder任务取出Opus数据解码为PCM → 同时JSON文本解析后通过LVGL显示在屏幕上（stt/tts/llm/iot不同类型分别处理）→ PCM数字信号送入ES8311经DAC转换为模拟音频 → 扬声器播放，用户听到AI回复。
	
	

	接收解码链路：云端返回的数据分为音频和文本两类。音频是Opus格式，通过websocket接到数据存入缓冲区内，再通过decoder任务解码为PCM后，把PCM数据送入ES8311 → **DAC（数模转换）** 转成模拟音频信号， 再把模拟信号输出到扬声器，让用户听到AI的回复；文本是JSON格式，我们针对不同类型的消息设置了对应的处理逻辑：

stt：语音转文字，将用户说的话显示在屏幕上用于确认识别准确性
tts：包含start（AI开始说话）、sentence_start（每句话开始）、stop（对话结束）三种状态，控制界面显示"说话中"或"聆听中"
llm：AI回复的表情包，显示在屏幕上增加交互趣味性
iot：设备控制指令，如调节音量、静音开关、控制LED灯等，通过解析JSON中的method和parameters执行对应操作
状态管理方面，我们设计了一个简单的状态机管理三种状态：IDLE（空闲等待唤醒）、LISTENING（聆听用户说话）、SPEAKING（AI播放回复）。状态切换由VAD（语音活动检测）和服务器消息共同触发，使用全局标志位communicationStatus进行控制。

UI界面使用LVGL实现，显示配网二维码，包含状态栏和对话主窗口。状态栏显示当前状态（配网中/聆听中/说话中）、WiFi连接状态、以及电量。主窗口显示用户语音文本（stt）和AI回复文本，AI回复使用30号字体，表情使用64号字体，状态栏使用20号字体。界面还实时显示时间和日期信息。

这就是整个AI小智项目的软硬件架构。

```









### 6.2 智能门铃

#### 项目背景

智能家居子项目，实现门铃 + 电子猫眼功能。

应用在智能家居，可以让用户通过摄像头观察门口情况；实现用户与门口人员的语音对讲以及OTA升级。

![](./images/电子门铃.PNG)

#### 系统组成

- **室外机**：摄像头、麦克风、按键，照明灯,电池供电

- **室内机**：显示屏、扬声器响铃 ，直接供电

- **APP 端**：远程查看、对讲

- **WebSocket 服务器**：信令中转

- **MQTT 服务器**：消息推送

  室内机室外机通过蓝牙 

  3000行代码  

  

#### 硬件配置

| 组件       | 型号        | 通信方式  |
| ---------- | ----------- | --------- |
| 主控       | ESP32-S3 c3 | -         |
| 音频编解码 | ES8311      | I2C + I2S |
| 功放       | NS4105B     | -         |
| 摄像头     | OV2640      | I2C + DVP |

接收器 使用c3 播放扬声器 

##### 音频相关

**音频采样率**   :1s进行多少次采样

**音频采样深度**:

​	每一次的采样通过ADC转换之后的结果是多少位[ADC位数]

**PCM** :原始的数字音频，没有经过任何压缩

**通道数**:  输入通道   几个麦克风  输出通道

**数据格式**: 菲利普格式  MSB格式  PCM帧同步格式

#### 软件功能

##### 1. 配网流程

首次启动使用 ESP-IDF 配网 API（BLE 方式）：

1. 建立蓝牙连接

2. 接收 WiFi 配置信息

3. 保存配置信息

4. 连接 WiFi.

5. 向 MQTT topic-A 发送 Ready 数据

6. 订阅 topic-B 接收后续指令

   ```
   开启启动->检查是否配过网YES ──► 直接连接已保存的Wi-Fi,NO ──► 进入配网模式 ──►手机连接BLE,- 写入SSID、密码──►ESP32校验数据并保存到NVS中──► 切换到WiFi - STA模式连接wifi──►成功后(wifi连接成功回调中初始化mqtt，再订阅topA),向 MQTT topic-A 发送 Ready就绪数据并订阅主题b接收后续app端指令， . 配网完成：关闭BLE资源，进入正常运行
   ```

   ```
   二维码内容:协议版本，BLE设备名称，安全密钥，传输方式
    对称加密 静态密钥  SECURITY_1
   
   ```

   

##### 2. 按铃流程

1. 通过 LoRa 向室内接收器发送通知 → 接收器响铃
2. 向 MQTT 服务器推送消息 → APP 收到通知
3. 主机本身响铃（反馈访客）

##### 3. 音视频通话

1. APP 向 MQTT 发送开启摄像头和麦克风指令
2. 主机收到指令后开始收集摄像头和麦克风数据
3. 将数据推送到 WebSocket 服务器
4. APP 获取门外音视频数据
5. APP 麦克风数据通过 WebSocket 推送到主机 → 扬声器播放

##### 4. OTA 升级

- 创建专用 OTA 任务，平时阻塞等待通知
- APP 检测新版固件后发送 OTA 指令
- 调用 `esp_https_ota` 下载固件
- 下载成功后调用 `esp_restart` 重启

**分区表**：ota_0 + ota_1 + ota_data



#### 常见面试问题

**Q1：音频采集流程中异步缓冲区大小？**

> 16*1024 byte，需多测试各种网络状态。大了延迟高，小了容易破音。

**Q2：视频采集为什么不采用异步？**

> 音频采用同步可能会丢数据影响用户体验，视频采用同步丢数据最多掉帧卡顿，可接受。

**Q3：蓝牙配网流程？是否支持 AP 配网？**

> 使用 ESP-IDF 提供的配网 API，支持两种方式：热点配网和低功耗蓝牙配网，项目中选择蓝牙方式。流程包括：建立蓝牙连接 → 接收 WiFi 配置 → 保存配置 → 连接 WiFi → 发送 Ready 数据到 MQTT。

**Q4：WebSocket 并发量和延迟？**

> 实际生活：几十到几百并发。延迟：亚秒级别，半秒到 1 秒，网络不好会卡顿。

采样率 16khz 采样深度 16位

**Q5：http和websocket的区别 ？**

>  **HTTP是请求-响应模式的短连接协议，WebSocket是全双工长连接协议，支持服务器主动推** 
>
> | 特性         | HTTP                         | WebSocket                        |
> | ------------ | ---------------------------- | -------------------------------- |
> | **连接方式** | 短连接，每次请求新建连接     | 长连接，握手后保持连接           |
> | **通信模式** | 单向：客户端请求→服务器响应  | **双向**：双方随时发送           |
> | **实时性**   | 差，需要轮询                 | **好**，服务器主动推送           |
> | **头部开销** | 大，每次请求500-1000字节     | 小，首次握手后仅2-14字节         |
> | **协议层次** | 应用层                       | 应用层（基于TCP）                |
> | **默认端口** | 80/443                       | 80/443（ws/wss）                 |
> | **适用场景** | 网页浏览、REST API、文件下载 | **实时通信**、音视频流、即时通讯 |

**Q6：对mqtt的理解**

| 特点          | 说明                                |
| ------------- | ----------------------------------- |
| **轻量级**    | 头部最小2字节，适合嵌入式设备       |
| **发布/订阅** | 发送方和接收方解耦，通过Topic通信   |
| **QoS机制**   | 0/1/2三种服务质量，平衡可靠性和性能 |
| **遗嘱消息**  | 设备异常断开时自动通知              |
| **保留消息**  | 新订阅者立即收到最新状态            |



**Q7：I2C 和 I2S 的区别？**

| 协议    | 场景           | 拓扑           | 工作方式      | 速率              | 线路                | 数据格式                   |
| ------- | -------------- | -------------- | ------------- | ----------------- | ------------------- | -------------------------- |
| **I2S** | 数字音频设备   | 点对点         | 全双工/半双工 | 快，可达数十 Mbps | 3-4 线：SD、WS、SCK | 固定为音频 PCM 数据        |
| **I2C** | MCU 和外围设备 | 多设备共享总线 | 半双工        | 慢，最快 3.4Mbps  | 2 线：SCL、SDA      | 任意：命令、配置、寄存器值 |

---

```
门铃视频与语音同步怎么实现：
两个任务异步并行 ，靠"自然延迟相近"实现软同步
视频任务
├── 独立运行，优先级8
├── 循环：采集JPEG → 发送WebSocket
└── 无时间戳标记

音频任务（ES8311Task + Buff2WsTask）
├── 独立运行，优先级8
├── 循环：采集音频 → 缓冲区 → 发送WebSocket
└── 无时间戳标记

两者关系：
├── 各自独立运行
├── 无同步信号
├── 无时间戳对齐
└── 靠"自然延迟相近"实现软同步

```

ble频率 2.4Ghz

5000mah  倍率：

#### lora：  esp—now

遇到的问题 

**第一个是音视频同步**。初期视频和音频独立传输，网络波动时延迟差异达到500ms，用户体验差。我分析原因是缺乏统一时间基准，于是添加了时间戳机制，接收端根据时间戳对齐播放，最终延迟差异控制在50ms以内。

**第二个是室外机低功耗**。电池供电续航只有3天，我发现WiFi功耗太高，于是改用ESP-NOW通信，加上深度睡眠和外设电源管理，最终续航提升到8-10个月

​		





#### 总结

```
五、智能门铃
这个门铃，分为门铃主机、室内接收器和手机APP三个部分，我们部门主要负责主机和接收器的开发。主机上有一个门铃按键、一个摄像头、一个麦克风，还有一个扬声器,一个补光灯。接收器上有一个扬声器和两个音量按键，两部分通过蓝牙进行通信。
这个门铃的使用逻辑很简单，就是当有人按下门铃时，室内接收器就会响铃，同时手机APP会收到一个通知，然后用户可以使用手机APP与访客进行语音通话，也可以查看门外的实时画面。
下面我介绍一下项目的硬件选型，门铃的主控选用的是乐鑫的ESP32-S3，这个芯片集成了WIFI、蓝牙，并且提供了I2S音频接口和DVP摄像头接口，所以很符合项目的需求，然后摄像头选用的是OV2640（使用DVP接口），音频编解码芯片使用的是ES8311（使用I2S接口），功放芯片选择的是NS4105B。
接收器的主控选择的是ESP32-C3，然后音频编解码芯片和功放芯片与主机都是相同的。
然后我再介绍一下这个项目的实现细节
首次启动门铃主机时，需要进行WIFI配网操作，需要使用手机app扫描设备机身上的二维码进行配网。这部分逻辑是使用ESP-IDF提供的配网API实现的，它提供了两种配网方式，一种是通过热点，另一种是通过低功耗蓝牙，我们选择的是第二种。这个配网API实现了配网所需的所有步骤，包括建立蓝牙连接、接收WIFI配置信息，保存WIFI配置信息等等。配网成功后，主机就会连接WIFI，然后向后台的MQTT服务器的一个topic-A发送一条Ready准备就绪数据，之后手机APP就会显示门铃已就绪，同时还会订阅另一个topic-B，用于接收后续手机APP端发送的指令。
然后就是日常使用，当有人按下门铃按钮时，门铃会执行以下逻辑。
首先通过LoRa向室内的接收器，发送通知，接收器收到通知后就会响铃。
然后还会向MQTT服务器推送一条消息，此时用户的手机APP就会收到一条通知：有人按门铃最后主机本身也会响铃，主要是告诉访客，门铃已经响应了，手机APP端再收到通知后，可以打开主机的摄像头和麦克风，查看访客身份，这功能实现逻辑也比较简单。首先手机APP会向MQTT服务器发送一个开启摄像头和麦克风的指令，发往的就是主机订阅的那个topic。当主机收到这个指令后，就会开始收集摄像头和麦克风数据，并将数据推送到一个WebSocket服务器，然后手机端就能获取门外的音视频数据了。另外，手机端app也可以收集手机这一端的麦克风数据和访客进行实时对讲，此时手机app会将mic数据推送到WebSocket服务器，并进一步推送到主机，主机在收到收据后，会使用扬声器播放。
以上就是核心功能的实现逻辑，除此之外我们还实现了OTA升级。
	这部分功能也是使用ESP-IDF提供的相关API实现的。我们创建了一个任务专门用于OTA升级，这个任务在平时会以阻塞的状态等待任务通知，当手机APP检测到有新版固件后，就会向MQTT服务器发送OTA升级的指令，门铃这边收到OTA指令后，就会向这个OTA任务发送通知，这个任务收到通知后，就会调用ESP-IDF提供的esp_https_ota函数，从我们指定的后台服务器，下载新版本的固件，下载成功后，再调用esp_restart，重启系统，这样就完成了OTA升级。
然后还需要补充一点，使用ESP-IDF提供的OTA功能时，需自己配置分区表，该分区表至少要包括两个 OTA 应用程序分区，分别是 ota_0 和 ota_1，和一个OTA数据分区 ota_data，ota_0和ota_1分别用于交替存储当前版本的固件和将来要下载的新版固件，而ota_data分区用于存储每次升级后，应该从哪个ota分区加载程序，这样一来，每次升级重启后，Bootloader就会根据ota_data中存储的值，去加载新版固件。
ota0和1 交替使用互为备份

	Flash 512KB 分区：
├─ Bootloader区：16KB   (0x08000000)
├─ 应用区：      200KB  (0x08004000)
├─ 下载区：      200KB  (0x08034000) ← 缓存从WiFi下载的固件
├─ 配置区：      4KB    (0x08066000) ← WiFi配置、更新标志
└─ 保留区：      92KB   (0x08067000)

特点：
├─ 应用区 = 下载区（200KB）
├─ 支持断点续传
├─ 更新安全
└─ 配置区存储WiFi账号密码

建议：物联网设备使用


```

 **Bootloader（引导加载程序）** 是嵌入式系统上电后最先运行的程序，负责**初始化硬件**并**加载应用程序**。 

OTA（Over-The-Air）空中升级技术，允许设备通过网络远程更新固件。

优势：
├── 远程修复Bug，无需现场更新
├── 快速迭代功能，提升用户体验
├── 降低维护成本，避免设备召回
└── 延长产品生命周期

esp32的ota成功后会标记为有效状态，失败后就会触发看门狗复位回滚

| 特性           | 一级Bootloader           | 二级Bootloader              |
| -------------- | ------------------------ | --------------------------- |
| **存储位置**   | ROM（芯片内部）          | Flash（外部存储）           |
| **地址**       | 0x40000000（ROM地址）    | 0x0（Flash起始地址）        |
| **是否可修改** | ❌ 不可修改，出厂固化     | ✅ 可配置，esp-idf生成       |
| **代码来源**   | 乐鑫出厂写入             | esp-idf编译生成             |
| **主要功能**   | 硬件初始化、加载二级Boot | 分区选择、加载APP、安全启动 |
| **大小**       | 约32KB                   | 约24KB                      |

```
OTA即Over-The-Air空中升级技术，允许设备通过网络远程更新固件。

以ESP32为例，其核心是双分区交替写入机制。Flash划分为ota_0和ota_1两个APP分区，当前运行A分区时，下载的固件写入B分区。写入完成后，更新OTA Data分区的状态标记为"待验证"，然后重启。

重启后Bootloader读取OTA Data，启动新固件。新固件启动后需要进行自检，自检成功则调用API标记为"有效"；如果自检失败或看门狗超时，Bootloader会自动回滚到旧固件。

这种设计保证了升级的原子性和可恢复性：要么成功升级，要么恢复旧版本，不会出现设备变砖的情况。

此外，ESP32还支持Secure Boot固件签名验证、Flash加密、版本号防降级等安全机制，确保升级过程安全可控。
```

```
环形缓冲区大小如何计算：
	环形缓冲区大小 = 数据速率(采样率 × 采样位深 × 单声道) × 最大延迟(300ms) × 安全系数3，你的音频项目32KB/s速率下，10KB可缓存约300ms，适合一般场景；网络波动大可增大到16-32KB。
```



```
WebSocket 核心优势                    │
├─────────────────────────────────────────────────────────┤
│  1. 全双工通信：客户端和服务器可同时发送数据              │
│  2. 低延迟：长连接避免了HTTP多次握手开销                  │
│  3. 服务器推送：服务器可主动推送数据到客户端              │
│  4. 轻量级头部：数据传输头部开销小                        │
│  5. 实时性好：适合音视频流、即时通讯等场景                │
	自动重连 
└───────────────────────────────────────────
```

```
低功耗 ：外设电源管理：摄像头、音频芯片用MOSFET控制，深度睡眠时彻底断电
不用WiFi直连：改用ESP-NOW通信，发送功耗从100mA降到30mA
快速启动：RTC Memory保存状态，唤醒后快速恢复，缩短运行时间
降频运行：CPU降到80MHz，降低运行功耗
立即休眠：传输完成马上进入深度睡眠，保持10μA
esp_deep_sleep_start()： 关闭cpu  rtc运行 按键中断唤醒
```

```
设备进入配网状态，开启BLE广播.App连接BLE，写入SSID、密码.
ESP32校验数据并保存到NVS中.切换到WiFi - STA模式连接路由器.成功后关闭BLE进入低功耗
```









### 6.7  **STM32步进电机闭环控制旋转平台**

#### 硬件架构

| 组件         | 型号             | 参数                                                         | 通信方式                 |
| ------------ | ---------------- | ------------------------------------------------------------ | ------------------------ |
| 主控         | STM32F103C8T6    | 主频 72MHz，Flash 64KB，RAM 20KB，ARM Cortex-M3 内核         | -                        |
| 电机驱动     | 东芝TB67S249FTG  | 最大电流 4.5A，支持 8 路细分选择(8分频，1600步一圈)，高细分驱动，每个上升沿走一步 | GPIO (脉冲+方向+8位并口) |
| **步进电机** | **57SM110**      | **两相四线，步距角 1.8°，额定电流 2.5A，保持扭矩 1.2N.m，机身长度 76mm**200步转一圈 | -                        |
| 编码器       | 增量式光电编码器 | 1000 线，AB 相输出，4 倍频后 4000 脉冲/圈，分辨率 0.09°      | 定时器编码器模式         |
| OLED 显示屏  | 2.42英寸SSD1309  | 128×64 分辨率，显示转速、角度、菜单界面                      | I2C                      |
| EEPROM 存储  | M24C02           | 256 字节，存储设备 ID 与校准参数                             | I2C                      |
| 电源连接器   |                  | 输入12V直流                                                  | I                        |
| 线性稳压器， | AMS1117          | 输出3.3V电压                                                 |                          |
| 开关稳压器   | TPS54360         | 输出5V电压                                                   |                          |

**esp32-c3**   

| **参**数     | 规格                    |
| ------------ | ----------------------- |
| **内核**     | RISC-V 32位单核，160MHz |
| **WiFi**     | 802.11 b/g/n，2.4GHz    |
| **蓝牙**     | BLE 5.0                 |
| **SRAM**     | 400KB                   |
| **Flash**    | 外置，最大16MB          |
| **UART**     | 2路                     |
| **SPI**      | 3路                     |
| **I2C**      | 1路                     |
| **USB**      | **USB-OTG全速**         |
| **工作电压** | 3.0V - 3.6V             |

### 1. 混合式结构

- **转子**：50齿永磁体（钕铁硼磁钢）
- **定子**：48齿，8个线圈（A相4个，B相4个）  
- **气隙**：0.05mm精密配合

### 2. 轴承配置

- 前轴承：6201ZZ（深沟球轴承）
- 后轴承：6001ZZ（编码器侧）
- 轴向间隙：≤0.05mm

 通过改变A+和A-的电流方向，A相可以产生**两个相反方向的磁场**，实现双向控制。 



**1000线增量式光电编码器（AB型）**

------

## 核心参数

| 参数         | 说明                                    |
| ------------ | --------------------------------------- |
| **线数**     | 1000 线/圈 → 转一圈输出 1000 个脉冲     |
| **相数**     | AB 两相（相位差 90°）                   |
| **分辨率**   | 1000×4 = **4000 脉冲/圈**（0.09°/脉冲） |
| **输出信号** | 方波脉冲（TTL 电平）                    |

| 模式        | 计数方式         | 脉冲数/圈 | 分辨率    |
| ----------- | ---------------- | --------- | --------- |
| 1倍频       | 只计A相上升沿    | 1000      | 0.36°     |
| 2倍频       | A相上下沿        | 2000      | 0.18°     |
| **4倍频** ✅ | **AB相全部边沿** | **4000**  | **0.09°** |

```c
编码器A相 ──→ TIM4_CH1 (PB6)
编码器B相 ──→ TIM4_CH2 (PB7)
// STM32定时器编码器模式自动处理方向识别
```

 编码器是1000线增量式AB相光电编码器，配合STM32定时器的**编码器模式3（4倍频）**，实现4000脉冲/圈的分辨率，定位精度达到**0.09度** 

##### 编码器核心参数

| 参数         | 说明                                    |
| ------------ | --------------------------------------- |
| **线数**     | 1000 线/圈 → 转一圈输出 1000 个脉冲     |
| **相数**     | AB 两相（相位差 90°）                   |
| **分辨率**   | 1000×4 = **4000 脉冲/圈**（0.09°/脉冲） |
| **输出信号** | 方波脉冲（TTL 电平）                    |

| 模式        | 计数方式         | 脉冲数/圈 | 分辨率    |
| ----------- | ---------------- | --------- | --------- |
| 1倍频       | 只计A相上升沿    | 1000      | 0.36°     |
| 2倍频       | A相上下沿        | 2000      | 0.18°     |
| **4倍频** ✅ | **AB相全部边沿** | **4000**  | **0.09°** |

modbus:通过字节之间的时间间隔确定 —— 若两个字节之间的时间间隔＞3.5 个字符时间，则认为一帧数据发送完成。



```
原因	说明
安全性	12V直流是安全特低电压，触摸无危险；220V有触电风险
电机特性	常用步进电机（如NEMA17/23）设计电压就是 12-48V 直流
驱动方式	步进电机需要 H 桥精确控制电流方向，直流更容易实现
控制精度	直流便于 PWM 调流，实现微步进细分控制
成本体积	220V 需要整流、滤波、隔离，电路复杂且体积大
步进电机不看电压看电流，关键是通过驱动器（如 A4988、DRV8825、TB6600）控制相电流大小来产生扭矩。12V 配合适当电流（如 1-2A）足以驱动大部分中小型步进电机。
```



ESP32 做热点（不需要外部 WiFi）

```
手机 ──WiFi直连──→ ESP32(AP热点) ──UART──→ STM32
"系统采用 MQTT 物联网协议 实现远程控制。

ESP32 作为 MQTT 客户端，连接到 EMQX 公共服务器（或阿里云 IoT）， 订阅 motor/control 主题接收手机命令，通过 UART 转发给 STM32 执行。

电机运行状态（角度、速度）通过 motor/status 主题实时上报， 手机端用微信小程序或 MQTT 客户端 APP 订阅显示。

这种架构的优势是手机和 ESP32 不需要在同一 WiFi，只要有网络就能控制， 适合远程监控场景。
```



#### 步进电机控制系统串讲

##### 一、项目概述与电机选型

我在做**高精度智能旋转平台**项目时负责电机驱动与控制部分。这个项目需要**精确的角度定位**（±0.1°精度），同时转速和负载属于中等水平，因此选择了**步进电机+梯形算法控制的方案。

我使用的电机型号是**57SM110混合式步进电机** 通用国产厂家，核心参数：**步距角**：1.8°/步（200步/圈）**保持扭矩**：1.2N.m，**额定电流**：2.5A，**机身长度**：76mm



##### 二、电机驱动原理

###### 1. 电机结构（混合式步进电机）

电机为**两相四线双极性**驱动，支持正反转：

- **定子**：48齿，由8个线圈组成（A相4个，B相4个）

- **转子**：50齿永磁体

-  定子48齿，转子50齿，**相差2齿**。这个差距使得每次切换AB相时，转子只能转动**1/4个齿距（1.8°）**，形成步进效果。如果齿数相同，转子会被完全吸住无法转动；差距设计让步进电机既有保持力矩又能精确步进 

- **驱动方式**：交替改变A+/A-和B+/B-的电流方向，每次转动1/4齿距（1.8°）

  ```
  **单相通电只能锁定转子，两相交替才能产生旋转磁场诱导转动**。通过A+→B+→A-→B-的顺序交替改变电流方向，定子磁场像"接力棒"一样引导转子一步步跟随，每步1.8°，这就是步进电机步进的原理 
  ```

 57SM110是**两相四线双极性**步进电机，通过**A+/A-和B+/B-两相线圈交替通电**产生旋转磁场。A相通电时转子对准0°，B相通电时对准90°，A相反向通电时对准180°，如此循环。驱动芯片TB67S249通过控制两相电流的大小和方向实现**1/8细分**，让转子在整步之间平滑过渡，每圈1600个脉冲完成360°旋转 



###### 2. 驱动芯片配置

使用东芝**TB67S249FTG**驱动芯片，支持**高细分控制**：

- 通过8路GPIO选择参考电流，实现**1/8细分**

- 200步/圈 × 8细分 = **1600脉冲/圈**

- 定位精度提升

  ```
  TB67S249FTG引脚          STM32连接           功能
  ─────────────────────────────────────────────────────
  STEP（脉冲输入）   ←──  PA8 (TIM1_CH1)     每来一个脉冲，电机走一步
  DIR（方向控制）    ←──  PA9               高电平正转，低电平反转
  ENABLE（使能）     ←──  GPIO控制          高电平使能电机（代码中的SD引脚）
  M0-M7（8位并行）   ←──  PA0-PA7           细分选择和电流设定
  
  ```

  

------

##### 三、STM32控制实现（核心代码）

###### 1. 速度控制 - 定时器比较输出

ARR自动重装载寄存器

使用**TIM1的PWM模式**生成步进脉冲：

使用011翻转模式 

在 定时器计数器值(CNT）==比较寄存器(CRR)的值 翻转电平，产生高电平，

一个方波溢出两次 

时钟：72mhz   进行72分频     即记一个数 1us

```c
// 速度转周期计算（代码中第53行附近）
// 速度单位：步/秒，定时器预分频72，计数周期1us
#define speed_to_cycle(x) (1000000 / (2 * x))

// 设置自动重装载值改变速度
__HAL_TIM_SET_AUTORELOAD(&htim1, (uint32_t)speed_to_cycle(currentSpeed) - 1);
```

011

 通过**TIM1输出比较模式**，设置**ARR寄存器**决定半周期长度，每两次中断（CCR匹配+ARR溢出）生成一个完整方波，脉冲频率由`speed_to_cycle()`公式计算，实现**速度数字化控制** 

**原理**：每个脉冲周期 = 1/速度，通过改变TIM1的ARR寄存器调整脉冲频率。

"**速度控制通过 STM32 的 TIM1 定时器输出比较模式实现**。

**原理层面**：电机转速由脉冲频率决定，我通过改变定时器的自动重装载值（ARR）来调整脉冲频率。具体来说，使用 `speed_to_cycle()` 公式将目标速度（步/秒）转换为 ARR 值，公式是 `1000000/(2×速度)`，其中乘以2是因为一个完整的方波周期需要两次定时器溢出中断。

**实现层面**：定时器预分频72，计数频率1MHz。假设目标速度1000步/秒，计算出ARR=500，即每500微秒溢出一次，两次溢出（1毫秒）完成一个方波周期，正好对应1000Hz的脉冲频率。

**调速层面**：在定时器中断回调中，我通过 `time_count` 计数器记录中断次数，每2次中断表示电机走了1步。同时调用 `updateSpeed()` 函数，根据当前处于加速段、匀速段还是减速段，实时计算新的速度值，并通过 `__HAL_TIM_SET_AUTORELOAD()` 动态更新ARR寄存器，实现电机转速的平滑变化。

**精度层面**：这种软件分频方式可以实现无级调速，配合梯形加减速算法，确保电机从800步/秒的起步速度平滑加速到目标速度，避免高速直接启动导致的丢步或堵转。"



根据起始速度计算设置重装载寄存器的值

在每一次定时溢出的时候,需要计算出下一步的速度,同时更新重装载寄存器的值

下一步速度计算：加速度阶段 =  上一步速度 + 加速度 * 时间

​								匀速阶段 =  上一步速度

​								减速阶段  =  上一步速度 -  加速度 * 时间

等到电机转动角度达到目标角度的时候停止定时器

###### 2. 加减速算法 - 梯形曲线

解决抖转的问题

自己定义了一个加速度

代码中实现了**自适应梯形加减速**（`Inf_Motor.c`第86-111行）：

![](D:\Program Files\feiq\Recv Files\面试整理\md\images\梯形算法.PNG)

```c
static void updateSpeed(void) {
    if (step_count < acc_step_count) {
        // 加速阶段：v = v0 + a*t
        currentSpeed += ACC_SPEED * (1.0 / currentSpeed);
    } else if ((angle_to_step(abs(targetAngle)) - step_count) <= acc_step_count) {
        // 减速阶段：v = v0 - a*t
        currentSpeed -= ACC_SPEED * (1.0 / currentSpeed);
    } else {
        // 匀速阶段
        currentSpeed = targetSpeed;
    }
}
```

**特点**：

- 根据目标角度**自动判断**三角形/梯形轨迹
- 加速度 = 1600 steps/s²，最小速度 = 800 steps/s
- 避免高转速堵转和丢步

"**梯形加减速算法是我这个项目的核心控制策略**。 需要高速转动的时候启用 

**原理上**，步进电机不能直接高速启动，否则会因为转子惯性跟不上磁场而丢步。梯形算法将运动分为三个阶段：匀加速、匀速、匀减速，让速度平滑过渡。

**实现上**，我通过运动学公式 `v² = v₀² + 2 a s(s:总距离)` 计算出加速到目标速度需要的步数。然后判断总距离是否足够：如果 `2×加速步数 > 总步数`，说明距离太短，采用三角形轨迹（直接加速到顶就减速）；否则采用梯形轨迹（有匀速段）。

**运行时**，在定时器中断的每步更新中，我根据当前步数判断处于哪个阶段：加速阶段用 `v += a×(1/v)` 递增速度，减速阶段递减，匀速阶段保持。同时加入限幅保护，确保速度不超过目标值也不低于最小值。

**效果上**，配合编码器反馈做终点校准，实际测试电机从800步/秒平滑加速到2000步/秒，定位精度达到±0.1度，完全没有丢步现象。"



#### 速度计算

stm32的tim1定时器 输出比较模式， 。每个脉冲由一个完整的方波周期组成，需要**两次定时器溢出中断**（一次高电平、一次低电平）。我通过修改**ARR自动重装载寄存器**来控制脉冲频率——ARR值越小，溢出越快，脉冲频率越高，电机转速越快 

arr溢出中断 

当 TIMx_CCR1 = TIMx_CNT 时，翻转 OC1REF 的电平    强制为有效电平  

在输出比较模式下，**CCR是设定的比较值**（通常固定为ARR的一半），**ARR才是根据速度动态修改的周期值**。

定时器CNT从0开始计数，**当CNT=CCR时，硬件自动翻转IO电平**（不需要进中断）；**当CNT=ARR时，计数器归零并产生更新中断**。



根据 初始目标的速度计算重装载寄存器arr的值，并产生中断更新计数器和arr

```
 方波生成使用**TIM1定时器的输出比较模式**。通过公式`speed_to_cycle()`将目标速度转换为ARR值，定时器每溢出两次（中断两次）产生一个完整方波周期，电机走一步。在**每次中断里更新下一步的速度值**，通过`__HAL_TIM_SET_AUTORELOAD()`实时改变ARR，实现脉冲频率的平滑调节，从而控制电机转速。 
```

arr溢出溢出一个中断，两个中断=一个完整方波周期=一步 

改变arr就是改变速度

```
ARR 就是"速度调节器"：ARR 越大，计数越久，脉冲越慢，电机越慢；ARR 越小，脉冲越快，电机越快。通过 __HAL_TIM_SET_AUTORELOAD() 实时改 ARR，就能让电机平滑加减速
```



###### 3. 位置控制 - 定时器中断计数

在**TIM1更新中断**中记录步数（第135-163行）：

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim) {
    if (htim->Instance == TIM1) {
        time_count++;
        if (time_count == 2) {  // 一个方波周期 = 2次中断
            time_count = 0;
            step_count++;        // 记录步数
            updateSpeed();       // 更新速度
            getCurrentAngleByEncoder();  // 编码器反馈
            
            // 到位检测
            if (currentAngle >= abs(targetAngle)) {
                Inf_Motor_Stop();
            }
        }
    }
}
```

------



###### 3. 角度计算（第120-129行）

```c
static void getCurrentAngleByEncoder(void) {
    if (targetAngle >= 0) {
        // 正转（向下计数）
        currentAngle = 360 * (time_over_flow_count * 65535 - 
                     __HAL_TIM_GET_COUNTER(&htim4)) / 4000;
    } else {
        // 反转（向上计数）
        currentAngle = 360 * (time_over_flow_count * 65535 + 
                     __HAL_TIM_GET_COUNTER(&htim4)) / 4000;
    }
}
```

**闭环策略**：

- **开环运行**：梯形加减速保证平稳性

  
- **丢步检测**：若编码器角度与理论值偏差>0.5°，可触发补偿（预留PID接口）

------







##### 五、远程控制 - ESP32-C3 MQTT方案

###### 1. 系统架构

**

```
手机APP          MQTT Broker         ESP32-C3           STM32
  │    ──WiFi──→    broker.emqx.io    ──WiFi──→     ──UART──→
  │    发布控制命令        转发消息        订阅topic      执行电机控制
  │    topic: motor/control              解析JSON       解析串口命令
  │                                                             
  │←──订阅状态────topic: motor/status←────上报角度/状态
```

###### 2. ESP32-C3功能（协处理器）

ESP32-C3负责**网络通信+协议转换**：

- **WiFi连接**：STA模式连路由器，或AP模式做热点
- **MQTT客户端**：订阅`motor/control`，发布`motor/status`
- **串口转发**：UART1（GPIO18/19）与STM32通信，115200波特率

###### 3. 通信协议设计

**下行控制命令**（APP → ESP32 → STM32）：

**json**









应用



```json
// MQTT格式
{
    "cmd": "move",      // move/stop/home
    "angle": 90.0,      // 目标角度
    "speed": 1000       // 目标速度
}

// ESP32转发给STM32的简化格式
"CMD:MOVE,ANGLE:90.0,SPEED:1000\n"
```

**上行状态上报**（STM32 → ESP32 → APP）：

**json**









应用



```json
{
    "state": "running",      // running/stopped/error
    "current_angle": 45.5,   // 编码器反馈角度
    "target_angle": 90.0,
    "speed": 1000
}
```

###### 4. STM32串口接收实现

**c**









应用



```c
// 串口中断中解析命令
void ProcessSerialCmd(char* cmd) {
    if (strstr(cmd, "CMD:MOVE")) {
        sscanf(cmd, "CMD:MOVE,ANGLE:%f,SPEED:%f", 
               &targetAngle, &targetSpeed);
        Inf_Motor_Start();  // 启动电机
    }
    else if (strstr(cmd, "CMD:STOP")) {
        Inf_Motor_Stop();   // 停止电机
    }
}
```

------

##### 六、项目亮点总结

| 技术点       | 实现方式            | 效果                            |
| ------------ | ------------------- | ------------------------------- |
| **运动控制** | 梯形加减速算法      | 自适应三角形/梯形轨迹，运行平稳 |
| **定位精度** | 1000线编码器×4倍频  | 0.09°分辨率，闭环校准           |
| **远程控制** | ESP32-C3 + MQTT     | 手机APP随时随地控制，无需现场   |
| **实时性**   | 定时器中断+FreeRTOS | 电机控制周期<1ms，响应及时      |
| **扩展性**   | 分层架构设计        | APP/Mid/Inf三层，便于功能扩展   |

"我的旋转平台设计了**双模人机交互架构**，兼顾现场操作和远程监控。

**本地控制**采用**4按键+2.42寸OLED**方案，基于FreeRTOS多任务设计，15ms周期扫描按键，400ms刷新显示。设计了三页面菜单：角度页、速度页、ID页，通过KEY4切换，KEY1/2调整参数，KEY3执行。设备ID和参数实时存EEPROM，掉电不丢失。这种方案**不依赖网络，响应快，适合现场调试和紧急停止**。

**远程控制**通过**ESP32-C3接入MQTT**，WiFi连接公共服务器（broker.emqx.io），手机微信小程序可以随时随地设定角度、调速、启停。通信协议采用JSON格式下行控制，简化格式UART透传给STM32；上行状态STM32打包为`A:角度,S:速度,STATE:状态`，ESP32转JSON后MQTT上报。实现了**跨网络实时控制**和**运行数据可视化**。

两套系统独立运行又互为备份：本地按键优先级最高，可覆盖远程命令；网络断开时自动切换本地模式，确保系统可靠性。"

| 等级  | 名称     | 说明               | 使用场景              |
| ----- | -------- | ------------------ | --------------------- |
| **0** | 最多一次 | 发完不管，可能丢失 | 高频采集（温度）      |
| **1** | 至少一次 | 确保送达，可能重复 | 控制命令（你的项目）✅ |
| **2** | 恰好一次 | 确保送达且不重复   | 支付、关键配置        |

 **遗嘱消息是MQTT的断线通知机制**。客户端连接时预设一条'遗言'，如果意外断线且超过KeepAlive时间未恢复，Broker会自动向指定主题发布这条消息。我的项目中，ESP32设置遗嘱为`{"state":"offline"}`，一旦WiFi断开或死机，手机APP能立即收到通知显示'电机离线'，避免用户误以为电机仍在正常运行 



##### 步进电机串讲总结

我之前在做XX项目中使用到了步进电机,我来负责对应电机驱动的部分。首先是对电机进行选型，因为这个项目需要进行精确的距离移动，同时在使用的过程中，转速和负载都是比较低的，特别适合使用步进电机驱动。我使用的这款电机是200步一圈，每一步对应的旋转角度是1.8°，对比BLDC电机，相对精度还是比较高的。
		我使用的是一款混合式步进电机，它是两相四线的双极性驱动，可支持正反转，它分为转子和定子，都带有齿轮结构，定子为48齿，由8个线圈组成，由两组通电导线共四根线，也就是A+,A-,B+,B-去控制它的磁性，转子为50齿，是永磁体，通过控制线圈的磁性就可以来控制电机的旋转。每个线圈对应6个齿，8组线圈六八四十八均匀分配，线圈之间存在缝隙，每个缝隙的宽度为1/4个齿，八组线圈加起来是两个齿，加上原有的48个齿，正好和内部转子的50个齿相对应，我们可以通过交替改变A相和B相的电流方向，来控制转子的受力，进而控制转子的旋转，转子每次只能转动1/4个齿，所以是200次转1圈，这就是步进电机内在的转动原理。
		接下来我来说一下如何控制这款步进电机的转速和运动步数，对于步进电机的转速控制，其本质是控制发送给电机的脉冲的频率，我在开发的时候还使用了东芝的TB电机驱动芯片，对运动步数可以进行进一步的细分，我这里选择了1/4步的分辨率，也就是给这个TB驱动芯片发送四个脉冲，电机转一步，结合电机本身是200个脉冲转一圈，这样就可以达到输出800个脉冲电机转一圈，可以实现更加精细的控制，我也只需要给TB芯片的一个引脚去发送脉冲，它会自动的转化为A相和B相的电流变化，降低了开发难度，至于我是如何去发送这个脉冲呢，我采用的是STM32定时器的一个比较输出，每跳变两次，算是一个脉冲，这样结合它的跳变周期就能计算出电机的转速，具体要结合定时器的时基单元和CCR寄存器的值去进行计算，而步数的控制呢，则是在定时器的比较中断里去进行记录，每两个中断去记录一步，数值达到800代表转过一圈，这就是对步进电机的转速和步数的控制。
但是步进电机它本身也是有缺点的，就是它适用于低转速和低负载的场景，原因是它在高转速时可能会发生堵转和丢步这两种情况，所以在启动和停止步进电机时，电机需要一个相对比较低的脉冲频率，不能突然启动到较高的转速，它可以有三种速度变化的算法可选，分别是梯形算法，S形算法和增量式PID控制算法，梯形算法和S形算法区别不大，都是分为加速阶段，匀速阶段和减速阶段，只不过梯形算法的加速和减速都是匀变速，也就是加速度保持不变，而S形算法的加速度是根据公式去先增加后减小的，从而让梯形的两个侧边变成一个S形的状态，
如果想要使用这个PID闭环控制算法，就需要去获取电机当前的位置，这里使用的是一款光电编码器，我使用的这款光电编码器是1000线的规格，对应着转一圈返回1000个信号，如果使用HAL库中配置了定时器自带的编解码器功能，选择了AB相同时采集变化沿，这样就实现了四倍频，也就是转过一圈之后会采集到4000个信号，由此去进行后续的闭环控制。这里使用的是增量式PID，根据传感器的信号，计算出当前的电机转动速度值，通过参数反馈调节，让电机有一个相对比较平滑的加减速曲线。





---

---

### 6.6 自行车码表

#### 一、 项目背景与概述

本项目是一款基于 **STM32** 微控制器的智能自行车码表，集成了 **GPS 导航**、**骑行数据监测**（速度、距离、坡度等）。

* **核心功能**：具备 GPS 轨迹记录与导航、实时运动数据显示（速度、里程、坡度等）、多语言切换以及深/浅色主题切换功能。
* **技术特点**：采用 **LVGL** 高性能图形库构建 UI，基于 **FreeRTOS** 进行多任务调度，实现了软硬件的高度解耦。

#### 二、 硬件架构选型

整个系统以 STM32 为核心，外挂多种传感器和人机交互模块。

##### 1. 主控模块

* **MCU**：STM32F405RGT6（主频 168MHz, Flash 1MB, SRAM 192KB）或 STM32H750(升级)
* **选型理由**：性能强劲，足以支撑 LVGL 高帧率渲染；丰富的外设接口满足多传感器连接需求。

##### 2. 传感器与外设

* **显示屏**：ST7789芯片 驱动的 2.4 英寸 TFT 彩色屏，分辨率 240×320，采用 SPI + DMA 通信以提高刷新率。


* **触摸屏**：触摸芯片：CST618D ，I2C 通信，在触摸时产生中断信号。


* **GPS 模块**：ATGM336H 中科微国产芯片，支持 BD+GPS 双模，1Hz 定位频率，通过串口回报 NMEA 协议数据。


* **陀螺仪**：LSM6DS 系列（如 LSM6DSMTR），I2C 通信，用于计算坡度及倾斜角。


* **存储与电源**：16G SD 卡存储路书；3.7V 三元锂电池（500-1500mAh），搭配 BQ24092 充电芯片，Type-C 接口充电，续航约 8-10 小时。

#### 三、 软件架构与技术栈

系统基于 STM32 标准库/HAL 库开发，采用模块化设计。

* **操作系统**：FreeRTOS
* **GUI 框架**：LVGL (Light and Versatile Graphics Library)
* **文件系统**：FatFS (用于 SD 卡存储路书、配置信息)
* **协议栈**：
* NMEA 0183 (GPS 数据解析)
* I2C (触摸屏、陀螺仪通信)
* SPI (屏幕驱动)

#### 四、 核心功能模块实现

##### 1. LVGL UI 设计与优化

* **模拟环境**：在 Windows 下搭建 GCC + CMake + SDL2 模拟环境进行 UI 开发。
* **多语言翻译**：利用 LVGL 静态翻译功能，通过 Tag 标签标识不同语言内容，并对中文字体进行裁剪以节省 Flash 空间。
* **深/浅色模式**：预设三套样式（浅色白色、浅色灰色、深色黑色），通过"观察者模式"绑定样式值，实现一键切换。
* **状态栏与页面**：顶部状态栏显示卫星数、运动状态和电量；内容区分为设置、骑行信息和路书三个 Tab 页。

##### 2. 瓦片地图系统 (路书功能)

* **原理**：采用 256×256 像素的瓦片地图，根据经纬度计算世界地图像素坐标及瓦片角标。
* **渲染逻辑**：默认加载以当前位置为中心的 **九宫格（9张）** 瓦片地图。用户拖动或位置移动时，动态计算并重新加载地图。
* **地图交互**：支持地图缩放、一键回到原点。
* **性能优化**：采用 display_buf 缓存（约 128K）确保至少能存放一屏数据；在 H7 平台上可使用 DMA2D 硬件加速。

##### 3. 软件任务划分

* **主任务**：负责 LVGL 图形库的渲染与 UI 交互。
* **电源任务**：检测开关机按键、测量电池电压（ADC）、读取充电状态，并异步更新 UI 上的电量图标。
* **GPS 任务**：获取并解析定位数据，进行坐标系转换（GCJ02），计算骑行速度、时间及里程，并更新地图位置。
* **坡度任务**：读取陀螺仪加速度数据，计算实时坡度。

#### 五、 项目难点与解决方案 (面试高频)

##### 1. 线程安全与 UI 更新

* **问题**：在多任务环境下直接修改 UI 主题数据可能导致死机。
* **解决方案**：
* 严禁在 `timer_handler` 处理中直接修改主题数据。
* 使用 `lv_async_call()` 将更新操作异步放入 UI 线程执行。
* 结合 FreeRTOS 的信号量互斥机制保障数据读写安全。

##### 2. LVGL 内存管理与渲染优化

* **问题**：STM32 内存有限，彩屏刷新数据量大，容易出现撕裂或卡顿。
* **解决方案**：
* 使用 **DMA (直接存储器访问)** 传输 SPI 数据，释放 CPU。
* 通过 SPI 理论速度（42M/s）配合 DMA 传输，满足 30Hz 以上的 UI 刷新需求。
* 地图图片链接仅在屏幕需要显示时才从 SD 卡读取渲染，减少内存占用。

##### 3. GPS 数据解析与坐标转换

* **问题**：GPS 原始数据存在噪点，需要进行坐标系转换（GCJ02）。
* **解决方案**：
* 解析 NMEA 协议数据，提取经纬度、速度、时间等信息。
* 进行坐标系转换，将 WGS84 坐标转换为 GCJ02 坐标，以匹配国内地图。

---







 

```

```

​			

 **NMEA 0183** 是GPS设备的标准通信协议，通过**串口(UART)**以ASCII文本格式输出定位数据。 

```
$GNGGA,123519,4807.038,N,01131.000,E,1,08,0.9,545.4,M,46.9,M,,*47
$GNRMC,123519,A,4807.038,N,01131.000,E,022.4,084.4,230394,003.1,W*6A
```

| 字段          | 含义                  |
| ------------- | --------------------- |
| `$GNGGA`      | 定位信息语句          |
| `$GNRMC`      | 推荐最小定位信息      |
| `4807.038,N`  | 纬度 (ddmm.mmmm格式)  |
| `01131.000,E` | 经度 (dddmm.mmmm格式) |
| `022.4`       | 地面速度(节)          |
| `08`          | 卫星数量              |

**C = 电池容量的倍数**

1C = 1小时放完电池的电流  放电电流250mA

```
BQ24092DGQT：单节锂电池线性充电管理IC
	获取引脚电压值计算当前电池电量；关键特性是Power Path架构，USB插入时同时给电池充电和系统供电，拔出时电池无缝接管，系统不会断电重启
```



#### 串讲模板

智能自行车码表项目串讲稿
开场（10秒）
我做的是一个智能自行车码表，主要功能是GPS导航，以及骑行速度，距离，时间等骑行数据统计并显示和能够通过触摸屏操作。

硬件架构（30秒）
硬件选型上，主控用STM32F405RGT6，Cortex-M4内核168MHz主频，flash 1M,sarm 192k。外设方面：
LCD屏幕：ST7789芯片 驱动的 2.4 英寸 TFT 彩色屏，分辨率 240×320，采用 SPI + DMA 通信以提高刷新
触摸芯片：CST618D ，I2C 通信，在触摸时产生中断信号
GPS模块使用的中科微电子ATGM336H 通过UART+DMA接收定位数据，定位频率1hz，
工作模式支持BD北斗+GPS双模，上电自动通过串口发送 NMEA 协议数据。(串口空闲中断回调，在gps任务中解析处理)
六轴陀螺仪（LSM6DS）通过I2C通信，用于计算实时坡度和倾斜角
SD卡存地图资源，电源管理做了软开关机电路
电源使用充电芯片：BQ24092DGQT，3.7v的三元锂电池：2000mAh，typec充电，续航时间6~8h

软件架构 - 使用hal库开发，创FreeRTOS多任务（40秒）
软件采用FreeRTOS多任务架构，创建了4个任务：

**主任务**：LVGL图形渲染，包括世界地图，实时运动信息，

**GPS任务**：解析GPS的定位NMEA数据获取经纬度、速度、卫星个数，处理经纬度数据, 并转换成GCJ02坐标，转换速度单位[节->km/h]，获取当前时间 -减去上一次获取定位数据，计算得到本次骑行时间,然后进行累加。6、根据计算的本次骑行时间×速度得到本次的骑行距离=进行累加得到总骑行距离。再进行异步更新lvgl骑行信息和用户再世界地图中的位置。
**坡度任务**：读取陀螺仪三轴的加速度，根据加速度计算出坡度(自行车Y轴加速度 / Z轴加速度)
**电源任务**：按键开关机检测 ，是否处于充电状态、ADC采样电压值， 满电4.2 空电3 ，再将获取的数据更新到lvgl





关键技术点：使用观察者模式（lv_subject）实现任务间解耦。比如GPS任务解析完数据后发布通知，UI自动订阅更新，避免多任务直接操作GUI导致的死锁问题。



**核心功能详解**（90秒）
界面分成标题栏和内容区两块：
标题栏显示卫星个数、骑行状态、电池电量。这三个数据来自不同任务，通过观察者模式异步更新。电池检测用ADC采集电压，同时检测充电状态做不同图标显示。

内容区是3个Tab页面：
**设置页面**： 通过lvgl中的静态翻译实现中英文切换(中文和对应的英文存储在翻译数组，回调触发全局替换)和深浅色主题切换( 我们设置了三套样式[ 浅色模式: 白色背景、灰色背景, 深色模式: 黑色背景 ], 将样式和绑定到观察者,一旦用户切换样式的时候, 修改观察者的值从而实现样式切换 )。主题系统用观察者+样式绑定设计，定义了3套样式对象，切换时修改观察者值，所有绑定组件自动重绘。
**骑行信息页面**： 通过主控读取GPS芯片定位信息(GNGGA和GNRMC语句)通过解析得到经纬度,速度,卫星个数,并且转换经纬度的坐标系,并且将GPS输出的WGS84坐标转为国内地图用GCJ02坐标系，我自己实现了WGS84到GCJ02的坐标转换算法。通过计算得到骑行时间、骑行距离，然后通过异步的形式更新到页面上 

**路书页面**：
		我们采用瓦片地图(整个1世界地图为一张大图，由多张小图片组成，一个小图片就是一张瓦片地图).一个瓦片地图256*256像素, 根据用户经纬度计算得到世界地图的像素角标以及瓦片地图的坐标、在一张瓦片地图中的相对像素角标。默认屏幕中心点就是用户所在位置。根据屏幕中心点加载9张瓦片地图,以屏幕中心点所在图片为九宫格的中心图片。一旦用户拖动地图的时候, 根据拖动的距离,计算出新的屏幕中心点所在的瓦片地图角标、世界像素角标、相对像素角标, 异步加载新的九宫格地图。后续一旦用户经纬度改变,用户位置信息随着改变，支持一键回原点，自动将用户位置置为屏幕中心。

​		性能优化：瓦片图片做LRU缓存，避免重复加载；使用异步加载防止UI卡顿 

​		地图缩放: 一旦缩放等级发送变化, 需要根据屏幕中心点的位置重新加载新的九宫格地图
​		回到用户原点按键: 一旦按下,缩放等级调到最大值, 此时用户所在位置就是屏幕中心点,重新加载新的九宫格地图 

**技术亮点总结**（20秒）
这个项目涉及GPS坐标转换算法、FreeRTOS任务同步、观察者设计模式、地图引擎开发。最难的是瓦片地图的坐标转换和动态加载逻辑，需要把经纬度转成世界像素坐标，再映射到具体瓦片和偏移量，涉及多层坐标系的转换。

结尾（10秒）
整个项目代码量大概3000行，以上就是自行车码表项目的软硬件架构



#### 面试官可能追问的问题 (模拟)

1. **Q: 瓦片地图系统是如何实现的？**

* A: 采用 256×256 像素的瓦片地图，根据经纬度计算世界地图像素坐标及瓦片角标。默认加载以当前位置为中心的九宫格（9张）瓦片地图，用户拖动或位置移动时，动态计算并重新加载地图。

2. **Q: 如何保证多任务环境下的 UI 线程安全？**

* A: 使用 `lv_async_call()` 将更新操作异步放入 UI 线程执行，并结合 FreeRTOS 的信号量互斥机制保障数据读写安全。严禁在 `timer_handler` 中直接修改主题数据。

3. **Q: 中文字体是如何处理的？**

* A: 利用 LVGL 的字体裁剪工具，仅保留项目需要用到的汉字，大幅缩减字体 bin 文件大小，节省 Flash 空间。

4. **Q: DMA2D 硬件加速在项目中如何应用？**

   A: 在 STM32H7 平台上，使用 DMA2D 进行图像的快速填充和拷贝，减轻 CPU 负担，提高 UI 渲染效率。

5. **在沙石地面骑行时，路面颠簸会导致加速度计产生高频振动噪声，直接计算坡度会剧烈跳动。需要滤波处理**： 

   ​	 开启芯片内部有数字低通滤波，沙石地面会产生高频振动噪声，我采用**滑动平均滤波**结合**GPS速度判断**来处理。具体是：20个采样点（400ms窗口）做移动平均，同时如果GPS速度低于0.5km/h认为坡度不可靠，保持上次的有效值。这样处理后，沙石路面的坡度显示从剧烈抖动（±20%）变为平滑变化（±2%），用户体验明显提升。 

   ```c
   #define SLOPE_WINDOW_SIZE 20  // 1秒窗口（50Hz采样）
   
   float slope_buffer[SLOPE_WINDOW_SIZE] = {0};
   uint8_t slope_index = 0;
   
   float calculate_smoothed_slope(float raw_slope) {
       // 存入缓冲区
       slope_buffer[slope_index] = raw_slope;
       slope_index = (slope_index + 1) % SLOPE_WINDOW_SIZE;
       
       // 计算平均值
       float sum = 0;
       for(int i=0; i<SLOPE_WINDOW_SIZE; i++) {
           sum += slope_buffer[i];
       }
       return sum / SLOPE_WINDOW_SIZE;
   }
   /* 这是一个环形缓冲区+滑动平均的滤波算法，用最新的20个数据点求平均，保留慢变化的真实坡度，滤除沙石路面的高频抖动噪声。
   为什么能滤除沙石噪声？
   沙石地面特点：
   高频抖动：每50ms采样，数值可能在±20%之间剧烈跳动
   真实坡度变化：缓慢变化，通常持续几秒钟才变化几度
   滑动平均效果：
   高频抖动（20ms周期）被20个点平均后，正负相互抵消
   缓慢趋势（秒级变化）被保留
   数学本质：低通滤波器，截止频率约 50Hz/20 = 2.5Hz  
    
   	
   ```

    **简化版卡尔曼滤波**
   估计值 = 预测值 + 卡尔曼增益 × (测量值 - 预测值)

   卡尔曼滤波是一种最优估计算法，核心思想是**预测+修正**。在自行车码表中，我可以用它融合加速度计和陀螺仪数据：加速度计长期准确但有抖动（沙石路面），陀螺仪短期稳定但有漂移。卡尔曼滤波通过动态调整权重（卡尔曼增益），平路时更相信加速度计，颠簸时更相信陀螺仪，比单纯滑动平均更智能，延迟也更小。 

   ​	卡尔曼滤波 vs 滑动平均

   | 特性         | 滑动平均             | **卡尔曼滤波**              |
   | ------------ | -------------------- | --------------------------- |
   | **原理**     | 历史数据平均         | 预测+测量融合               |
   | **延迟**     | 大（窗口越大越滞后） | 小（根据置信度动态调整）    |
   | **适应性**   | 固定权重             | **自适应权重**（K动态变化） |
   | **多传感器** | 不支持               | **天然支持融合**            |
   | **复杂度**   | 简单                 | 较复杂（需要调Q、R参数）    |
   | **计算量**   | O(n)或O(1)           | O(1)，但矩阵运算            |

​	

瓦片地图缓存满了怎么处理？

​		 *当缓存满时，找最久未使用的瓦片替换* 

FreeRTOS任务优先级怎么设计的？

- UI渲染最高（用户交互不能卡）
- 传感器数据次之（GPS 1秒一次足够）
- 电源管理最低（只是轮询检测）

坐标转换算法具体怎么实现的？ WGS84 -->GCJ02

```c
void wgs84_to_gcj02_f(float wgs_lat, float wgs_lon, float *gcj_lat, float *gcj_lon) {
    // 1. 判断是否在中国境内
    if(is_out_of_china_f(wgs_lat, wgs_lon)) {
        *gcj_lat = wgs_lat;
        *gcj_lon = wgs_lon;
        return;
    }   
    // 2. 计算偏移量（国测局非线性算法）
    float d_lat = transform_lat_f(wgs_lon - 105.0f, wgs_lat - 35.0f);
    float d_lon = transform_lon_f(wgs_lon - 105.0f, wgs_lat - 35.0f);  
    // 3. 地球曲率修正
    float rad_lat = wgs_lat / 180.0f * PI_F;
    float magic = 1.0f - EE_F * sinf(rad_lat) * sinf(rad_lat);
    float sqrt_magic = sqrtf(magic);  
    // 4. 偏移量转角度
    d_lat = (d_lat * 180.0f) / ((A_F*(1.0f-EE_F)) / (magic*sqrt_magic) * PI_F);
    d_lon = (d_lon * 180.0f) / (A_F / sqrt_magic * cosf(rad_lat) * PI_F);
    // 5. 应用偏移
    *gcj_lat = wgs_lat + d_lat;
    *gcj_lon = wgs_lon + d_lon;
}
```

**软开关电路的原理是什么**？ 硬件工程师实现

​	**核心：N-MOS + P-MOS + MCU自锁**

| 状态     | 过程                                               |
| -------- | -------------------------------------------------- |
| **开机** | 按键→P-MOS导通→N-MOS导通→MCU上电→MCU拉高PWR_EN自锁 |
| **维持** | MCU保持PWR_EN高→N-MOS持续导通→松手也不断电         |
| **关机** | MCU检测按键→拉低PWR_EN→N-MOS截止→系统断电          |

卡顿解决?

​		 阻塞式读取

​			使用dma异步读取sd卡数据  预加载相邻瓦片 使用8m sram缓存多张更多张9宫格地图数据，加载时直接从缓存读取，

```
时间轴：
0ms   ────────────────────────────────────── 900ms
      [加载9张图] UI冻结                      显示完成
      
用户体验：卡死1秒
时间轴：
0ms   ─ 10ms ─ 110ms ─ 210ms ─ 310ms ... 900ms
      │       │       │       │
      │       │       │       └─ 加载第3张（后台）
      │       │       └─ 加载第2张（后台）
      │       └─ 加载第1张（后台）
      └─ 立即显示（缓存+路径）
      
用户体验：瞬间响应，图片逐渐清晰
```



```
你的SD卡使用SDIO 4-bit协议（PC8~PC12），比SPI快4倍，读取速度约12MB/s。
SD卡协会定义的高速并行接口标准
卡顿不是SD卡速度慢，而是FATFS文件系统多次打开/关闭的开销问题
1. 命令阶段 (CMD线)
   Host发送: CMD17 (读单块) + 块地址
   Card响应: 48-bit响应

2. 数据阶段 (DATA0-3)
   Card发送: Start Bit + 512字节数据 + CRC + End Bit
   使用4根线并行传输

3. DMA搬运 (不占用CPU)
   SDIO → DMA → SRAM (内存)
```







---

## 附录A：项目串讲模板

### 一、智能门锁项目串讲

这个智能门锁项目支持多种开锁方式，包括**密码开锁、指纹开锁和手机App蓝牙开锁**，并且支持**OTA升级**功能。用户交互方面还提供了语音播报提示和键盘背光功能。

**硬件选型**：

- 主控：ESP32-C3（支持WIFI/蓝牙，集成FreeRTOS，自带esp-idf框架）
- 触摸键盘：SC12B（I2C通信，支持中断）
- 指纹模组：FPM383（UART通信，中断通知，一体化设计）
- 语音模块：WTN6170（一线串口）
- 全色灯：WS2812（RMT驱动）

**开锁方式实现**：

1. **密码开锁**：
   - 12键电容触摸键盘通过I2C与主控通信
   - 支持中断通知：按键触摸时芯片拉高中断引脚
   - 外部中断ISR与任务通过**任务通知**同步
   - 密码以`#`结尾，对比成功后GPIO驱动电机开锁

2. **指纹开锁**：
   - 一体化指纹模组（内置传感器+识别芯片）
   - UART通信，支持中断通知
   - 任务通知机制：阻塞等待→中断触发→查询结果→开锁

3. **蓝牙开锁**：
   - 基于ESP-IDF蓝牙驱动
   - 配对后发送特定指令完成开锁/管理

**OTA升级**：

- 专用OTA任务，阻塞等待通知
- 手机APP触发→连接WIFI→`esp_https_ota`下载固件→`esp_restart`重启
- 分区表：ota_0 + ota_1 + ota_data
- Bootloader根据ota_data选择加载分区

**低功耗设计**：

- `DFS + Auto Light-Sleep + Bluetooth Modem-sleep`
- 唤醒源：GPIO（触摸/指纹中断）
- 续航：半年到两年多

---

### 二、智能门铃项目串讲

这个门铃分为**门铃主机、室内接收器和手机APP**三个部分。主机有门铃按键、摄像头、麦克风和扬声器；接收器有扬声器和音量按键；两部分通过**LoRa**通信。

**硬件选型**：

- 主机：ESP32-S3（240MHz，512KB RAM，8MB Flash，16MB SPIRAM）
- 摄像头：OV2640（DVP接口）
- 音频编解码：ES8311（I2C控制+I2S数据）
- 功放：NS4105B
- 接收器：ESP32-C3

**实现细节**：

1. **配网**：
   - 使用ESP-IDF配网API，选择**BLE配网**
   - 流程：蓝牙连接→接收WIFI配置→保存→连接WIFI→MQTT发送Ready

2. **按铃流程**：
   - LoRa向接收器发通知→接收器响铃
   - MQTT推送消息→APP收到通知
   - 主机本身响铃（反馈访客）

3. **音视频通话**：
   - APP通过MQTT发送开启指令
   - 主机采集摄像头+麦克风数据→WebSocket服务器
   - APP获取音视频数据；APP麦克风数据→WebSocket→主机播放

4. **OTA升级**：
   - 专用OTA任务等待通知
   - `esp_https_ota`下载→`esp_restart`重启
   - 双分区设计（ota_0/ota_1）+ ota_data

---

### 三、AI小智项目串讲

AI聊天机器人是**嵌入式前端项目**，进行人声识别并与云端AI大模型交互，实现日常聊天和设备操控。

**硬件选型**：

- 主控：ESP32-S3（240MHz，512KB RAM，16MB Flash）
- 音频编解码：ES8311（I2C+I2S，采样率16000Hz，16位）
- 屏幕：ST7789V（320×240 IPS，SPI接口）

**软件架构**：

**上行链路**（人声→服务器）：

```
麦克风→ES8311(PCM)→Feed任务→环形缓存→Fetch任务
→Opus编码→输出缓存→WebSocket→云端
```

**下行链路**（云端响应→播放）：

```
WebSocket接收→Opus解码→PCM→ES8311→扬声器播放
```

**核心任务**：

- **Feed任务**：识别人声，PCM数据存放到环形缓存
- **Fetch任务**：数据传递给Opus编码器
- **解码任务**：Opus解码为PCM播放

**回调函数类型**：

- **STT**：Sound To Text，屏幕显示识别文字
- **TTS**：Text To Sound，包含start/sentence_start/stop
- **LLM**：AI回复表情包显示
- **IOT**：根据JSON执行硬件控制（调音量、开关灯等）

**状态机**：语音识别状态→唤醒状态→睡眠状态（全局标志位切换）

**UI显示（LVGL）**：

- 状态栏：状态机状态、电池、WiFi（20号字模）
- 会话窗口：聊天记录（30号字体）
- 表情包：64号字体

---

### 四、畜牧定位器项目串讲

畜牧定位器对农场牛羊进行**定位**和**计步**监测健康状态。

**主体逻辑**：定期获取GPS定位和步数→联网上传→APP/小程序查看统计。

**两种联网方案**：

1. **NB-IoT直接联网**：信号好时使用
2. **LoRa网关转发**：信号差时使用

**硬件选型**：

- 主控：STM32F103C8T6（72MHz，实际使用8MHz）
- GPS模块：AT6558R-5N32（串口，GPS+北斗双模）
- 计步模块：DS3553（IIC，内置加速度传感器+算法）
- NB-IoT：QS100（串口AT指令）
- LoRa：LLCC68（SPI，433MHz）
- 电池：ER-26500锂亚电池，9000mAh，续航半年到一年

**数据获取**：

- **GPS**：上电后不断获取，主动发送给主控，提取RMC信息（经纬度+速度+时间）
- **计步**：DS3553自动识别脚步并计数，主控通过IIC读取

**低功耗设计**：

- 主控：待机模式（RTC闹钟唤醒）
- NB-IoT：AT指令进入低功耗
- GPS：直接断电，唤醒后重新上电
- 计步模块：静止15s自动进入低功耗
- 主频从72MHz降低到8MHz

---

### 五、STM32网关项目串讲

网关为**不具备联网能力的设备提供联网能力**，支持RS485、LoRa、CAN与下游设备通信，以太网/NB-IoT与云端交互。

**硬件选型**：

- 主控：STM32F103C8T6（成本低、生态成熟、接口丰富）
- RS485：MAX13487EESA+（Modbus-RTU协议）
- CAN：PD1050S（自定义协议）
- LoRa：LLCC68（SPI通信）
- 以太网：W5500（SPI，MQTT协议）
- NB-IoT：QS100（串口AT指令）

**软件架构**：

**任务划分**：

- **网络任务**：MQTT连接，上传数据，获取远端命令
- **RS485处理任务**：封装协议帧，下发指令，接收响应
- **LoRa处理任务**：LoRa通信，自定义协议
- **CAN处理任务**：CAN通信，差分信号收发

**协议格式**：

- **Modbus-RTU**：设备地址(1B) + 功能码(1B) + 数据(NB) + CRC(2B)
- **LoRa自定义**：帧头校验 + 命令内容 + 帧尾校验（启动/停止/转向/转速）
- **CAN**：使用报文ID标识电机和网关

**实现细节**：

- MQTT客户端注册订阅远端服务器主题
- 回调函数使用cJSON解析字符串，提取控制指令
- RS485：USART串口通信，自定义指令拼接
- LoRa：移植LLCC68驱动，发送后进入接收模式等待返回
- CAN：差分信号收发，不同ID标识

---

### 六、步进电机控制串讲

**电机选型**：

- 类型：混合式步进电机
- 规格：两相四线双极性，200步/圈，1.8°/步
- 结构：定子48齿（8组线圈），转子50齿（永磁体）

**转动原理**：

- 交替改变A相和B相电流方向控制转子受力
- 转子每次转动1/4个齿，200次转1圈
- 使用东芝TB驱动芯片，1/4步细分：800脉冲/圈

**控制方法**：

- **转速控制**：控制脉冲频率（STM32定时器比较输出）
- **步数控制**：定时器比较中断中计数，每两个中断记录一步

**加减速算法**：

- **梯形算法**：加速度恒定，匀变速
- **S形算法**：加速度先增后减，曲线平滑
- **增量式PID**：配合编码器实现闭环控制

**编码器反馈**：

- 1000线光电编码器
- 四倍频（AB相同时采集变化沿）：4000脉冲/圈
- 增量式PID调节，平滑加减速曲线

**适用场景**：低转速、低负载。高转速可能导致堵转和丢步。

---

## 附录B：C语言速查表

| 知识点               | 要点                                                      |
| -------------------- | --------------------------------------------------------- |
| **源码到可执行文件** | 预处理→编译→汇编→链接                                     |
| **内存模型**         | 栈（函数调用）、堆（malloc）、数据区（全局/静态）、代码区 |
| **堆栈区别**         | 堆自下而上、慢、面向用户；栈自上而下、快、面向OS          |
| **字节序**           | ARM小端序：`*(char *)&i == 1`判断                         |
| **指针和引用**       | 指针可不初始化，引用必须初始化且不可更改                  |
| **回调函数**         | 函数指针作为参数，满足条件时调用（MQTT/蓝牙回调）         |
| **volatile**         | 防止编译器优化，强制从内存读取                            |
| **内存泄漏**         | malloc未free；**内存溢出**OOM直接报错                     |

---

## 附录

### 常用缩写对照

| 缩写 | 全称                                        | 中文                 |
| ---- | ------------------------------------------- | -------------------- |
| ADC  | Analog-to-Digital Converter                 | 模数转换器           |
| DAC  | Digital-to-Analog Converter                 | 数模转换器           |
| PWM  | Pulse Width Modulation                      | 脉冲宽度调制         |
| I2C  | Inter-Integrated Circuit                    | 集成电路总线         |
| SPI  | Serial Peripheral Interface                 | 串行外设接口         |
| UART | Universal Asynchronous Receiver/Transmitter | 通用异步收发传输器   |
| GPIO | General Purpose Input/Output                | 通用输入输出         |
| RTOS | Real-Time Operating System                  | 实时操作系统         |
| BLE  | Bluetooth Low Energy                        | 低功耗蓝牙           |
| NVS  | Non-Volatile Storage                        | 非易失性存储         |
| OTA  | Over-The-Air                                | 空中升级             |
| PID  | Proportional-Integral-Derivative            | 比例-积分-微分控制   |
| MQTT | Message Queuing Telemetry Transport         | 消息队列遥测传输协议 |
| DMA  | Direct Memory Access                        | 直接内存访问         |

---

*整理时间：2025年*